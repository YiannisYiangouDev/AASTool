// Building types are now fetched from the backend API (/api/v1/building-types).
// This file only retains the type for backward compatibility with any legacy code.
export type BuildingType = string;

